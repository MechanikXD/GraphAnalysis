namespace Core.LoadSystem
{
    public struct LocalRoot
    {
        // TODO: Interactive loading of NEW data (for node position adjusting)
        // TODO: Save data (for rule-defined use JSON and store only node data || for specific use matrix)
        // TODO: Loading system (from JSON || from matrix, CSV including)
    }
}